#include "Types.hpp"

namespace ayla::language {

void register_types(celestia::LanguageDefinition &def) {

  // semantic::BuiltinTypes builtin;

  // builtin.Unknown = def.types.add("unknown", new Type(...));

  // builtin.Number = def.types.add("number", new Type(...));

  // builtin.String = def.types.add("string", new Type(...));

  // builtin.Boolean = def.types.add("bool", new Type(...));

  // builtin.Char = def.types.add("char", new Type(...));

  // builtin.Null = def.types.add("null", new Type(...));

  // builtin.Void = def.types.add("void", new Type(...));
}

} // namespace ayla::language